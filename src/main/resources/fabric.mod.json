{
  "schemaVersion": 1,
  "id": "hyprrank",
  "version": "${version}",
  "name": "HyprRank",
  "description": "HyprRank replaces rank text (Member, VIP, MVP, Owner...) in chat and the tab list with colorful glyphs/textures, fully syncable from the server.",
  "authors": ["Satanas1275"],
  "contact": {
    "sources": "https://github.com/Satanas1275/HyprRank",
    "issues": "https://github.com/Satanas1275/HyprRank/issues"
  },
  "license": "CC-BY-NC-4.0",
  "icon": "assets/hyprrank/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": ["com.hyprank.HyprRank"],
    "client": ["com.hyprank.HyprRankClient"]
  },
  "mixins": ["hyprrank.mixins.json"],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": ">=${minecraft_version}",
    "fabric-api": ">=${fabric_version}",
    "java": ">=${java_version}"
  }
}
